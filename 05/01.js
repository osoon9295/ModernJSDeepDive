var score1 = 50 + 50; // 표현식

var score2 = 100; // 표현식

score; // 표현식

// 리터럴 표현식
10;
("Hello");

// 식별자 표현식(선언이 이미 존재한다고 가정)
sum;
personalbar.name;
arr[1];

// 연산자 표현식
10 + 20;
sum = 10;
sum !== 10;

// 함수/메서드 호출 표현식(선언이 이미 존재한다고 가정)
square();
person.getName();

var x = 1 + 2;
// 식별자 표현식 x는 3으로 평가된다.
x + 3; // 6
